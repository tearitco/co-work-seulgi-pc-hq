# terrain_types.txt - real, hand-curated registry, mirroring mutaclysm's
# own pieces/registry/terrain/terrain_types.txt convention exactly
# (system/chtpm_rgb_render.c's load_emoji_colors_from(), pipe-delimited:
# glyph|asset_id|name|walkable|rgb_top|unicode|rgb_top_emoji).
#
# 2026-08-02: board-viewer had NO registry file at all, meaning its own
# 2D emoji rendering relied entirely on chtpm_rgb_render's GENERIC
# on-demand path (ensure_emoji_asset_generated) rather than this real,
# proven hand-curated path every other project in this house uses -
# per that function's own header comment (chtpm_rgb_render.c:284-306):
# "a hand-curated table hit short-circuits the generic path entirely"
# and documents a real, previously-live-caught failure mode of relying
# on the generic path alone. asset_id values below match the real,
# already-generated pieces/registry/emoji_assets/<id>/voxels_16.csv
# directories (same hex-codepoint ids bv_render_3d.c's own
# terrain_asset_hex() uses) - same real texture data, now reached via
# the reliable registry path instead of the on-demand one.
# Format: glyph|asset_id|name|walkable|rgb_top|unicode|rgb_top_emoji
.|1F33E|Plains|1|120,170,80|🌾|120,170,80
f|1F332|Forest|1|30,110,40|🌲|30,110,40
^|26F0|Hills|1|140,120,100|⛰|140,120,100
~|1F30A|Water|0|40,90,180|🌊|40,90,180
C|1F3F0|Capital|1|200,180,60|🏰|200,180,60
